{
  "name": "course-generator-fullstack",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/server.js"
  }
}